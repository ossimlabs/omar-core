pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-core-plugin'

project(':omar-core-plugin').projectDir = "${rootProject.projectDir}/plugins/omar-core-plugin" as File
